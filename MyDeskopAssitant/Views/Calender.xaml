<UserControl x:Class="MyDeskopAssitant.Views.Calender"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MyDeskopAssitant.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="{DynamicResource PrimaryBackgroundColor}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Margin="20" Background="{DynamicResource SecundaryBackgroundColor}" CornerRadius="10" Padding="10">
            <Viewbox Stretch="Uniform">
                <Calendar SelectedDate="{Binding SelectedDate}" 
                          SelectionMode="SingleDate"
                          Background="Transparent"
                          BorderThickness="0"/>
            </Viewbox>
        </Border>

        <Grid Grid.Column="1" Margin="0,20,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding SelectedDate, StringFormat='dd MMMM yyyy, dddd', ConverterCulture='tr-TR'}" 
                       Foreground="{DynamicResource PrimaryTextColor}" 
                       FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

            <ListView Grid.Row="1" ItemsSource="{Binding DisplayEvents}" 
                      Background="Transparent" BorderThickness="0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource SecundaryBackgroundColor}" CornerRadius="5" Margin="0,5" Padding="10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding Title}" FontSize="16" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextColor}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsPast}" Value="True">
                                                    <Setter Property="Foreground" Value="Gray"/>
                                                    <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Grid Grid.Row="2" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Text="{Binding EventInput, UpdateSourceTrigger=PropertyChanged}" 
                         Height="40" VerticalContentAlignment="Center" Padding="5"
                         FontSize="14" Margin="0,0,10,0"/>

                <Button Grid.Column="1" Command="{Binding AddEventCommand}" 
                        Content="Ekle" Width="80" Height="40"
                        Style="{StaticResource MusicControlButton}" Background="#444"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl> 
