<UserControl x:Class="MyDeskopAssitant.Views.TodoList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyDeskopAssitant.Views"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Background="{DynamicResource PrimaryBackgroundColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Margin="10" CornerRadius="10" Background="{DynamicResource SecundaryBackgroundColor}" Height="50">
            <TextBlock Text="To-Do List" FontSize="24" 
                       Foreground="{DynamicResource PrimaryTextColor}" 
                       FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Border>

        <Border Grid.Row="1" Margin="10,0,10,10" Background="{DynamicResource SecundaryBackgroundColor}" CornerRadius="5" Padding="5">
            <DockPanel LastChildFill="True">
                <TextBlock Text="Search By:" Foreground="{DynamicResource SecundaryIconColor}" VerticalAlignment="Center" Margin="5,0"/>

                <ComboBox SelectedValue="{Binding SearchType}" SelectedValuePath="Content" Width="100" Margin="5,0" Height="30">
                    <ComboBoxItem Content="All" IsSelected="True"/>
                    <ComboBoxItem Content="Id"/>
                    <ComboBoxItem Content="Content"/>
                    <ComboBoxItem Content="Date"/>
                </ComboBox>

                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                         Height="30" VerticalContentAlignment="Center" Padding="5"/>
            </DockPanel>
        </Border>

        <Border Grid.Row="2" Margin="10,0,10,10" Background="{DynamicResource SecundaryBackgroundColor}" CornerRadius="5" Padding="5">
            <ListView ItemsSource="{Binding DisplayTasks}" Background="Transparent" BorderThickness="0">

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Border Background="{TemplateBinding Background}" Padding="5" BorderThickness="0,0,0,1" BorderBrush="#333">
                                        <GridViewRowPresenter/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Id" Width="40">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Id}" Foreground="{DynamicResource PrimaryTextColor}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Task" Width="300">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Content}" VerticalAlignment="Center" FontSize="14">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{DynamicResource PrimaryTextColor}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                        <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                        <Setter Property="Foreground" Value="Gray"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Date" Width="90">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding TaskDate}" Foreground="{DynamicResource SecundaryIconColor}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Done" Width="60">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsCompleted}" 
                                              Command="{Binding DataContext.CheckCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Edit" Width="60">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}" Content="Edit" Height="25"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Del" Width="60">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}" Content="X" Height="25" Background="#FF5555" Foreground="White" BorderThickness="0"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <Border Grid.Row="3" Margin="10,0,10,10" Background="{DynamicResource SecundaryBackgroundColor}" CornerRadius="5" Padding="10">
            <DockPanel LastChildFill="True">
                <Button DockPanel.Dock="Right" Content="Add Task" 
                        Command="{Binding AddCommand}"
                        Width="100" Height="30" Margin="10,0,0,0"
                        Style="{StaticResource MusicControlButton}" Background="#444"/>

                <TextBox Text="{Binding NewTaskContent, UpdateSourceTrigger=PropertyChanged}" 
                         Height="30" VerticalContentAlignment="Center" Padding="5" 
                         FontSize="14" Tag="Enter new task..."/>
            </DockPanel>
        </Border>

        <Grid Grid.RowSpan="4" Background="#AA000000" Visibility="{Binding IsEditPopupOpen, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Width="300" Height="180" Background="{DynamicResource SecundaryBackgroundColor}" CornerRadius="10" Padding="20" VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Edit Task" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextColor}" Margin="0,0,0,15"/>

                    <TextBox Text="{Binding EditContent, UpdateSourceTrigger=PropertyChanged}" Height="35" FontSize="14" VerticalContentAlignment="Center" Margin="0,0,0,15"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Save" Command="{Binding SaveEditCommand}" Width="80" Height="30" Margin="0,0,10,0" Background="{DynamicResource PrimaryBlueColor}" Foreground="White"/>
                        <Button Content="Cancel" Command="{Binding CancelEditCommand}" Width="80" Height="30"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

    </Grid>
</UserControl>